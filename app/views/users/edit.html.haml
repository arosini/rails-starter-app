-title = @user == current_user ? t('users.edit.self_title') : t('users.edit.other_title', user_id: @user.id)

- content_for :head do
  %title= title

#grid-wrap
  %h2.page-title.well.center= title
  = render "form", user: @user
  = back_btn
